import { combineReducers } from "redux";

import shows from "./movies";
import wishlist from "./wishlist";
// import search from './search';

// export default combineReducers({shows, search, wishlist});
export default combineReducers({shows, wishlist});